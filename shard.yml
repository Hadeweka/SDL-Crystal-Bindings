name: sdl-crystal-bindings
version: 0.3.0

authors:
  - Hadeweka

description: |
  Raw bindings to SDL function, mostly recommended for writing wrappers.

license: MIT

crystal: '>= 1.6.0'

libraries:
  libSDL2: ~> 2.0
  libSDL2_image: ~> 2.0
  libSDL2_mixer: ~> 2.0
  libSDL2_ttf: ~> 2.0